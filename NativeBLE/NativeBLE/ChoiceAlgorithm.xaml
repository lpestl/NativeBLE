<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NativeBLE.Core.Forms.ChoiceAlgorithm">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0" Padding="16" Spacing="16" VerticalOptions="Center">
                <Label Text="Selected algorithm connection: " />
                <Picker x:Name="algorithmTypePicker" Title="Choose the connection algorithm" SelectedIndex="0" />
            </StackLayout>

            <Button Grid.Row="1"
			    FontSize="24" Text="Save" IsEnabled="{Binding IsSelectionComplete}"
				Clicked="OnSelectionSaved">
                <Button.Margin>
                    <OnPlatform x:TypeArguments="Thickness" iOS="0,10" Android="0" />
                </Button.Margin>
            </Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>